'use client';import{useMemo,useState}from'react';import type{Business,Service,WorkingHour}from'@/lib/types';import'./tenant.css';
type Busy={start_at:string;end_at:string;staff_id?:string|null};const pad=(n:number)=>String(n).padStart(2,'0'),iso=(d:Date)=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`,hm=(t:string)=>{const[a,b]=t.slice(0,5).split(':').map(Number);return a*60+b},fmt=(m:number)=>`${pad(Math.floor(m/60))}:${pad(m%60)}`;const names=['Pazar','Pazartesi','Salı','Çarşamba','Perşembe','Cuma','Cumartesi'],months=['Oca','Şub','Mar','Nis','May','Haz','Tem','Ağu','Eyl','Eki','Kas','Ara'];
export default function TenantBooking({business,services,hours,staff,staffServices,staffHours}:{business:Business;services:Service[];hours:WorkingHour[];staff:any[];staffServices:any[];staffHours:any[]}){const[step,setStep]=useState(1),[service,setService]=useState<Service|null>(null),[staffId,setStaffId]=useState(''),[date,setDate]=useState(''),[time,setTime]=useState(''),[busy,setBusy]=useState<Busy[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState(''),[cancelUrl,setCancelUrl]=useState('');const t={serviceTitle:'Hizmet seç',serviceDescription:'Almak istediğin hizmeti seç.',staffTitle:'Çalışan seç',staffDescription:'Dilersen bir uzman seç veya ilk uygun çalışanı tercih et.',dateTitle:'Gün ve saat seç',dateDescription:'Sana uygun günü ve saati seç.',formTitle:'Bilgilerini gir',formDescription:'Randevunu tamamlamak için bilgilerini gir.',firstNameLabel:'Ad',lastNameLabel:'Soyad',phoneLabel:'Telefon',emailLabel:'E-posta',noteLabel:'Not',consentText:'Bilgilerimin randevu amacıyla işlenmesini kabul ediyorum.',submitText:'Randevuyu Oluştur',successTitle:'Görüşmek üzere!',successDescription:'Randevun başarıyla oluşturuldu.',...(business.booking_texts||{})};const matchedStaff=service?staff.filter(s=>staffServices.some(x=>x.staff_id===s.id&&x.service_id===service.id)):[],eligible=matchedStaff.length?matchedStaff:(service?staff:[]),visibleEligible=eligible.filter(s=>!s.is_default&&s.title!=='Ana Takvim'&&s.username!=='ana-takvim');const days=useMemo(()=>Array.from({length:14},(_,i)=>{const d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+i);const ids=staffId&&staffId!=='any'?[staffId]:eligible.map(s=>s.id),businessOpen=hours.find(h=>h.day_of_week===d.getDay())?.is_open,open=ids.some(id=>{const own=staffHours.find(h=>h.staff_id===id&&h.day_of_week===d.getDay());return own?own.is_open:businessOpen});return{value:iso(d),name:names[d.getDay()],n:d.getDate(),month:months[d.getMonth()],open}}),[staffId,staffHours,eligible,hours]);const slots=useMemo(()=>{if(!service||!date||!staffId)return[];const day=new Date(date+'T12:00:00').getDay(),ids=staffId==='any'?eligible.map(s=>s.id):[staffId],out=[];for(let m=0;m<1440;m+=business.slot_interval){const start=new Date(`${date}T${fmt(m)}:00+03:00`),end=new Date(start.getTime()+service.duration_minutes*60000);if(start.getTime()<Date.now()+300000)continue;const available=ids.some(id=>{const own=staffHours.find(h=>h.staff_id===id&&h.day_of_week===day),wh=own||hours.find(h=>h.day_of_week===day);return wh?.is_open&&m>=hm(wh.start_time)&&m+service.duration_minutes<=hm(wh.end_time)&&!busy.some(b=>(!b.staff_id||b.staff_id===id)&&start<new Date(b.end_at)&&end>new Date(b.start_at))});if(available)out.push(fmt(m))}return out},[service,date,staffId,busy,eligible,staffHours,hours,business.slot_interval]);async function chooseDate(v:string){setDate(v);setTime('');setLoading(true);const r=await fetch(`/api/appointments?businessId=${business.id}&date=${v}`),j=await r.json();setBusy(j.busy||[]);setLoading(false)}async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!service||!date||!time)return;setLoading(true);setError('');const f=new FormData(e.currentTarget),r=await fetch('/api/appointments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({businessId:business.id,serviceId:service.id,staffId:staffId==='any'?null:staffId,date,time,firstName:f.get('firstName'),lastName:f.get('lastName'),phone:f.get('phone'),email:f.get('email'),note:f.get('note')})}),j=await r.json();setLoading(false);if(!r.ok)setError(j.error);else{setCancelUrl(j.cancelUrl);setStep(5)}}const trDate=date?new Intl.DateTimeFormat('tr-TR',{weekday:'long',day:'numeric',month:'long'}).format(new Date(date+'T12:00:00')):'';return <div className={`tenantBooking booking-style-${business.booking_ui_style||'cards'} option-shape-${business.booking_option_shape||'rounded'} option-style-${business.booking_option_style||'filled'} density-${business.booking_density||'comfortable'} action-style-${business.booking_action_style||'solid'} action-anim-${business.booking_action_animation||'none'}`}><div className="tenantProgress">{[1,2,3,4].map(n=><i key={n} className={n<=step?'active':''}/>)}</div>{error&&<p className="tenantError">{error}</p>}{step===1&&<div><Step n="01" title={t.serviceTitle} description={t.serviceDescription}/><div className="tbServices">{services.map(s=><button key={s.id} className={service?.id===s.id?'selected':''} onClick={()=>{setService(s);setStaffId('')}}><i/><span><b>{s.name}</b><small>{s.duration_minutes} dakika</small></span>{business.show_prices&&s.price!==null&&<strong>{Number(s.price).toLocaleString('tr-TR')} ₺</strong>}</button>)}</div><Next disabled={!service} onClick={()=>{if(visibleEligible.length){setStep(2)}else{setStaffId('any');setStep(3)}}}>{visibleEligible.length?'Çalışan Seç →':'Gün ve Saat Seç →'}</Next></div>}{step===2&&<div><Back onClick={()=>setStep(1)}/><Step n="02" title={t.staffTitle} description={t.staffDescription}/><div className="staffOptions"><button className={staffId==='any'?'selected':''} onClick={()=>setStaffId('any')}><span className="staffAvatar">✓</span><div><b>Fark etmez</b><small>İlk uygun çalışan atansın</small></div></button>{visibleEligible.map(s=><button key={s.id} className={staffId===s.id?'selected':''} onClick={()=>setStaffId(s.id)}>{s.photo_url?<img src={s.photo_url} alt={s.name}/>:<span className="staffAvatar">{s.name.charAt(0)}</span>}<div><b>{s.name}</b><small>{s.title||'Uzman'}</small></div></button>)}</div><Next disabled={!staffId} onClick={()=>setStep(3)}>Gün ve Saat Seç →</Next></div>}{step===3&&<div><Back onClick={()=>setStep(visibleEligible.length?2:1)}/><Step n="03" title={t.dateTitle} description={t.dateDescription}/><div className="tbDays">{days.map(d=><button key={d.value} disabled={!d.open} className={`${date===d.value?'selected':''} ${!d.open?'closed':''}`} onClick={()=>chooseDate(d.value)}><small>{d.name}</small><b>{d.n}</b><span>{d.month}</span></button>)}</div>{date&&<><h4>{trDate} · Uygun saatler</h4><div className="tbSlots">{loading?<p>Yükleniyor…</p>:slots.length?slots.map(s=><button className={time===s?'selected':''} key={s} onClick={()=>setTime(s)}>{s}</button>):<p>Uygun saat kalmadı.</p>}</div></>}<Next disabled={!time} onClick={()=>setStep(4)}>Bilgilerimi Gir →</Next></div>}{step===4&&<form onSubmit={submit}><Back onClick={()=>setStep(3)}/><Step n="04" title={t.formTitle} description={t.formDescription}/><div className="tbTwo"><label>{t.firstNameLabel}<input name="firstName" required/></label><label>{t.lastNameLabel}<input name="lastName" required/></label></div><label>{t.phoneLabel}<input name="phone" required/></label><label>{t.emailLabel} <small>(iptal bağlantısı için)</small><input name="email" type="email" required={business.booking_require_email}/></label>{business.booking_show_note!==false&&<label>{t.noteLabel}<textarea name="note" rows={3}/></label>}<label className="tbConsent"><input type="checkbox" required/>{t.consentText}</label><button className="tenantNext" disabled={loading}>{loading?'Kaydediliyor…':`${t.submitText} →`}</button></form>}{step===5&&<div className="tbDone"><span>✓</span><h3>{t.successTitle}</h3><small>{t.successDescription}</small><p>{service?.name}<br/>{trDate} · {time}</p>{cancelUrl&&<a className="cancelManageLink" href={cancelUrl}>Randevuyu görüntüle veya iptal et</a>}</div>}</div>}
function Step({n,title,description}:{n:string;title:string;description?:string}){return <div className="tbTitle"><span>{n}</span><div><h3>{title}</h3>{description&&<p>{description}</p>}</div></div>}function Next({children,...p}:any){return <button className="tenantNext" {...p}>{children}</button>}function Back({onClick}:any){return <button type="button" className="tbBack" onClick={onClick}>← Geri dön</button>}
