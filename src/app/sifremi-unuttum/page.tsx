'use client';import'@/app/admin-modern.css';import{useState}from'react';import{createClient}from'@/lib/supabase-browser';
export default function Forgot(){const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get('email'));const configured=process.env.NEXT_PUBLIC_SITE_URL||'',base=/^https?:\/\//i.test(configured)?configured.replace(/\/$/,''):location.origin;await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${base}/sifre-yenile`});setMsg('Şifre yenileme bağlantısı e-posta adresine gönderildi.')}return <main className="verifiedPage"><section><form onSubmit={submit}><p className="overline">HESAP KURTARMA</p><h1>Şifremi unuttum.</h1>{msg&&<p className="formSuccess">{msg}</p>}<label className="field">E-posta<input className="input" type="email" name="email" required/></label><button className="formButton">Bağlantı Gönder</button></form></section></main>}
