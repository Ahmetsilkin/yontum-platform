import{NextRequest,NextResponse}from'next/server';import{createClient,createServiceClient}from'@/lib/supabase-server';import{z}from'zod';
const schema=z.object({businessId:z.string().uuid(),name:z.string().trim().min(2),username:z.string().trim().min(2).max(30),email:z.string().email(),phone:z.string().optional(),title:z.string().optional(),bio:z.string().optional(),photoUrl:z.string().url().optional().or(z.literal('')),serviceIds:z.array(z.string().uuid()).min(1)});
export async function POST(req:NextRequest){try{const x=schema.parse(await req.json()),auth=await createClient(),{data:{user}}=await auth.auth.getUser();if(!user)return NextResponse.json({error:'Giriş gerekli.'},{status:401});const{data:m}=await auth.from('business_members').select('role').eq('business_id',x.businessId).eq('user_id',user.id).single();if(!m||!['owner','manager'].includes(m.role))return NextResponse.json({error:'Yetkiniz yok.'},{status:403});const db=createServiceClient(),site=process.env.NEXT_PUBLIC_SITE_URL||req.nextUrl.origin,{data:invite,error:inviteError}=await db.auth.admin.inviteUserByEmail(x.email,{redirectTo:`${site}/auth/callback?next=/sifre-olustur`,data:{invited_staff:true,business_id:x.businessId}});if(inviteError||!invite.user)return NextResponse.json({error:inviteError?.message||'Davet gönderilemedi.'},{status:400});const{data:staff,error}=await db.from('staff_profiles').insert({business_id:x.businessId,user_id:invite.user.id,name:x.name,username:x.username,email:x.email,phone:x.phone||null,title:x.title||null,bio:x.bio||null,photo_url:x.photoUrl||null}).select('id').single();if(error)return NextResponse.json({error:error.message},{status:400});await db.from('business_members').insert({business_id:x.businessId,user_id:invite.user.id,role:'staff'});await db.from('staff_services').insert(x.serviceIds.map(service_id=>({staff_id:staff.id,service_id})));const{data:hours}=await db.from('working_hours').select('day_of_week,is_open,start_time,end_time').eq('business_id',x.businessId);if(hours?.length)await db.from('staff_working_hours').insert(hours.map(h=>({...h,staff_id:staff.id})));return NextResponse.json({ok:true,staffId:staff.id},{status:201})}catch(e){if(e instanceof z.ZodError)return NextResponse.json({error:'Bilgileri kontrol edin.'},{status:400});return NextResponse.json({error:'Beklenmeyen hata.'},{status:500})}}
