'use client';import'../admin-modern.css';import{useState}from'react';import Link from'next/link';import{createClient}from'@/lib/supabase-browser';
export default function Login(){const[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget),db=createClient(),{data:signInData,error}=await db.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error){setError('E-posta veya şifre hatalı.');setLoading(false)}else{if(signInData.user)await db.from('staff_profiles').update({has_logged_in:true}).eq('user_id',signInData.user.id).eq('has_logged_in',false);location.href='/panel'}}return <main className="authPage"><section className="authSide"><Link className="platformLogo inverse" href="/"><span>Y</span><b>YONTUM<small>İŞLETMEN CEBİNDE.</small></b></Link><h1>İŞLETMEN<br/>CEBİNDE</h1><small>Güvenli giriş · Supabase Auth</small></section><section className="authMain"><form className="authCard" onSubmit={submit}><Link className="platformLogo" href="/"><span>Y</span><b>YONTUM</b></Link><h2>Tekrar hoş geldin.</h2><p>İşletmeni yönetmek için giriş yap.</p>{error&&<p className="formError">{error}</p>}<label className="field">E-posta<input className="input" type="email" name="email" required/></label><label className="field">Şifre<input className="input" type="password" name="password" required/></label><p className="forgotLink"><Link href="/sifremi-unuttum">Şifremi unuttum</Link></p><button className="formButton" disabled={loading}>{loading?'Giriş yapılıyor…':'Giriş Yap →'}</button><p className="authSwitch">Hesabın yok mu? <Link href="/kayit">Ücretsiz başla</Link></p></form></section></main>}
